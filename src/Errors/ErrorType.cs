namespace Result;

// Error types
public enum ErrorType
{    
    Failure = 0,
    Validation = 1,
    NullValue = 3,
    NotFound = 4,
    Unauthorized = 5,
    Forbidden = 6,
    Unexpected = 7    
}
